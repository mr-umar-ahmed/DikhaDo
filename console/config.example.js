// Copy to config.js (git-ignored) and fill in. The anon key is a public client key; the URL + key pair is still kept out of git
// because the hackathon database has open row-level security.
window.DIKHADO_CONFIG = { url: "https://YOUR-PROJECT.supabase.co", anonKey: "YOUR-ANON-KEY" };
